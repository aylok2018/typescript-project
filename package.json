{
  "name": "pr4",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "live-server --open=index.html --port=5500"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "live-server": "^1.2.2"
  }
}
